# Legacy flat dependency list for reference or unusual workflows.
# Normal installs should use pyproject.toml, for example:
#   pip install ".[opensearch]"
# See README.md — Installation & Setup.
# Python 3.9+

pyyaml>=6.0

# OpenSearch export
opensearch-py>=2.0.0

# Optional: for future Horreum implementation (currently stubbed)
requests>=2.28.0

# Optional: For better performance
# ujson>=5.0.0

# Tests
pytest>=7.0.0
